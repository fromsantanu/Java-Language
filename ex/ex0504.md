# Here are focused exercises on **Inheritance and Method Overriding** in Java:

---

### **Exercise 1: Basic Inheritance**
1. Create a class `Animal` with:
   - A method `speak()` that prints: `"Animal makes a sound."`

2. Create a class `Dog` that extends `Animal`:
   - Override the `speak()` method to print: `"Dog barks."`

3. In the `main()` method:
   - Create objects of both `Animal` and `Dog`.
   - Call `speak()` on both objects.

---

### **Exercise 2: Using `super` to Call Parent Methods**
1. Modify the `Dog` class from Exercise 1:
   - Add a method `superSpeak()` that calls the parent `speak()` method using `super`.

2. In the `main()` method:
   - Call `superSpeak()` on a `Dog` object to observe the output.

---

### **Exercise 3: Multilevel Inheritance**
1. Create a class `Vehicle` with:
   - A method `move()` that prints: `"Vehicle is moving."`

2. Create a class `Car` that extends `Vehicle`:
   - Override `move()` to print: `"Car is moving on four wheels."`

3. Create a class `ElectricCar` that extends `Car`:
   - Override `move()` to print: `"Electric car is moving silently."`

4. In the `main()` method:
   - Create objects of `Vehicle`, `Car`, and `ElectricCar`.
   - Call `move()` on each object.

---

### **Exercise 4: Polymorphism**
1. Modify the classes `Animal` and `Dog`:
   - Add a class `Cat` that extends `Animal` and overrides the `speak()` method to print: `"Cat meows."`

2. In the `main()` method:
   - Create an array of `Animal` type to store objects of `Animal`, `Dog`, and `Cat`.
   - Use a loop to call `speak()` on each object.

---

### **Exercise 5: Method Overriding with Additional Behavior**
1. Create a class `Employee` with:
   - A method `work()` that prints: `"Employee is working."`

2. Create a class `Manager` that extends `Employee`:
   - Override `work()` to print: `"Manager is managing the team."`
   - Add a method `delegate()` that prints: `"Manager is delegating tasks."`

3. In the `main()` method:
   - Create a `Manager` object and call both `work()` and `delegate()`.

---

### **Exercise 6: Using `super` for Constructor Chaining**
1. Create a class `Person` with:
   - A constructor that accepts `String name` and prints: `"Person's name is: [name]"`.

2. Create a class `Employee` that extends `Person`:
   - A constructor that accepts `String name` and `String role` and calls the parent constructor using `super`.

3. In the `main()` method:
   - Create an `Employee` object and observe the output.

---

### **Exercise 7: Abstract Classes and Method Overriding**
1. Create an abstract class `Shape` with:
   - An abstract method `draw()`.

2. Create subclasses `Circle` and `Rectangle` that extend `Shape`:
   - Override `draw()` to print specific messages for each shape.

3. In the `main()` method:
   - Create an array of `Shape` type to store objects of `Circle` and `Rectangle`.
   - Use a loop to call `draw()` on each object.

---

### **Exercise 8: Method Overriding with Type Casting**
1. Create a class `Parent` with:
   - A method `display()` that prints: `"This is the parent class."`

2. Create a class `Child` that extends `Parent`:
   - Override `display()` to print: `"This is the child class."`

3. In the `main()` method:
   - Create a `Parent` reference pointing to a `Child` object.
   - Call `display()` using the reference.
   - Use explicit casting to call `display()` from the child class.

---

### **Exercise 9: Preventing Method Overriding**
1. Create a class `FinalDemo` with:
   - A `final` method `display()` that prints: `"This method cannot be overridden."`

2. Create a subclass of `FinalDemo` and try to override `display()`:
   - Observe the compilation error.

---

### **Exercise 10: Dynamic Method Dispatch**
1. Create a class `Bank` with:
   - A method `getInterestRate()` that returns `0.0`.

2. Create subclasses `HDFCBank` and `ICICIBank` that extend `Bank`:
   - Override `getInterestRate()` to return specific interest rates (e.g., `5.5` for HDFC, `6.0` for ICICI).

3. In the `main()` method:
   - Create a `Bank` reference pointing to objects of `HDFCBank` and `ICICIBank`.
   - Call `getInterestRate()` using the `Bank` reference and observe the output.

---

These exercises cover various aspects of **Inheritance** and **Method Overriding** in Java. Let me know if you’d like solutions for these!

## Here are the solutions to the exercises on **Inheritance and Method Overriding** in Java:

---

### **Solution 1: Basic Inheritance**
```java
class Animal {
    void speak() {
        System.out.println("Animal makes a sound.");
    }
}

class Dog extends Animal {
    @Override
    void speak() {
        System.out.println("Dog barks.");
    }
}

public class Main {
    public static void main(String[] args) {
        Animal animal = new Animal();
        animal.speak();

        Dog dog = new Dog();
        dog.speak();
    }
}
```

---

### **Solution 2: Using `super` to Call Parent Methods**
```java
class Animal {
    void speak() {
        System.out.println("Animal makes a sound.");
    }
}

class Dog extends Animal {
    @Override
    void speak() {
        System.out.println("Dog barks.");
    }

    void superSpeak() {
        super.speak(); // Calling the parent class method
    }
}

public class Main {
    public static void main(String[] args) {
        Dog dog = new Dog();
        dog.superSpeak(); // Calls the parent class speak() method
    }
}
```

---

### **Solution 3: Multilevel Inheritance**
```java
class Vehicle {
    void move() {
        System.out.println("Vehicle is moving.");
    }
}

class Car extends Vehicle {
    @Override
    void move() {
        System.out.println("Car is moving on four wheels.");
    }
}

class ElectricCar extends Car {
    @Override
    void move() {
        System.out.println("Electric car is moving silently.");
    }
}

public class Main {
    public static void main(String[] args) {
        Vehicle vehicle = new Vehicle();
        vehicle.move();

        Car car = new Car();
        car.move();

        ElectricCar electricCar = new ElectricCar();
        electricCar.move();
    }
}
```

---

### **Solution 4: Polymorphism**
```java
class Animal {
    void speak() {
        System.out.println("Animal makes a sound.");
    }
}

class Dog extends Animal {
    @Override
    void speak() {
        System.out.println("Dog barks.");
    }
}

class Cat extends Animal {
    @Override
    void speak() {
        System.out.println("Cat meows.");
    }
}

public class Main {
    public static void main(String[] args) {
        Animal[] animals = { new Animal(), new Dog(), new Cat() };

        for (Animal animal : animals) {
            animal.speak(); // Polymorphic behavior
        }
    }
}
```

---

### **Solution 5: Method Overriding with Additional Behavior**
```java
class Employee {
    void work() {
        System.out.println("Employee is working.");
    }
}

class Manager extends Employee {
    @Override
    void work() {
        System.out.println("Manager is managing the team.");
    }

    void delegate() {
        System.out.println("Manager is delegating tasks.");
    }
}

public class Main {
    public static void main(String[] args) {
        Manager manager = new Manager();
        manager.work();
        manager.delegate();
    }
}
```

---

### **Solution 6: Using `super` for Constructor Chaining**
```java
class Person {
    Person(String name) {
        System.out.println("Person's name is: " + name);
    }
}

class Employee extends Person {
    Employee(String name, String role) {
        super(name); // Calls the parent class constructor
        System.out.println("Role: " + role);
    }
}

public class Main {
    public static void main(String[] args) {
        Employee employee = new Employee("John", "Manager");
    }
}
```

---

### **Solution 7: Abstract Classes and Method Overriding**
```java
abstract class Shape {
    abstract void draw();
}

class Circle extends Shape {
    @Override
    void draw() {
        System.out.println("Drawing a circle.");
    }
}

class Rectangle extends Shape {
    @Override
    void draw() {
        System.out.println("Drawing a rectangle.");
    }
}

public class Main {
    public static void main(String[] args) {
        Shape[] shapes = { new Circle(), new Rectangle() };

        for (Shape shape : shapes) {
            shape.draw(); // Polymorphic behavior with abstract methods
        }
    }
}
```

---

### **Solution 8: Method Overriding with Type Casting**
```java
class Parent {
    void display() {
        System.out.println("This is the parent class.");
    }
}

class Child extends Parent {
    @Override
    void display() {
        System.out.println("This is the child class.");
    }
}

public class Main {
    public static void main(String[] args) {
        Parent parentRef = new Child();
        parentRef.display(); // Calls the overridden method

        Child child = (Child) parentRef; // Explicit type casting
        child.display(); // Calls the child class method
    }
}
```

---

### **Solution 9: Preventing Method Overriding**
```java
class FinalDemo {
    final void display() {
        System.out.println("This method cannot be overridden.");
    }
}

class SubClass extends FinalDemo {
    // Uncommenting the below code will cause a compilation error
    // @Override
    // void display() {
    //     System.out.println("Trying to override.");
    // }
}

public class Main {
    public static void main(String[] args) {
        SubClass obj = new SubClass();
        obj.display();
    }
}
```

---

### **Solution 10: Dynamic Method Dispatch**
```java
class Bank {
    double getInterestRate() {
        return 0.0;
    }
}

class HDFCBank extends Bank {
    @Override
    double getInterestRate() {
        return 5.5;
    }
}

class ICICIBank extends Bank {
    @Override
    double getInterestRate() {
        return 6.0;
    }
}

public class Main {
    public static void main(String[] args) {
        Bank bank1 = new HDFCBank();
        System.out.println("HDFC Interest Rate: " + bank1.getInterestRate());

        Bank bank2 = new ICICIBank();
        System.out.println("ICICI Interest Rate: " + bank2.getInterestRate());
    }
}
```

---

These solutions illustrate various aspects of **Inheritance** and **Method Overriding** in Java. Let me know if you need further assistance!
