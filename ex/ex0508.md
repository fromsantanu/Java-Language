# Here are focused exercises on **Abstraction** (using abstract classes and interfaces) in Java:

---

### **Exercise 1: Abstract Class Basics**
1. Create an abstract class `Animal` with:
   - An abstract method `makeSound()`.
   - A concrete method `eat()` that prints: `"Animal is eating."`

2. Create a subclass `Dog` that extends `Animal`:
   - Implement the `makeSound()` method to print: `"Dog barks."`

3. In the `main()` method:
   - Create a `Dog` object and call both `makeSound()` and `eat()`.

---

### **Exercise 2: Abstract Class with Multiple Subclasses**
1. Create an abstract class `Shape` with:
   - An abstract method `calculateArea()`.

2. Create subclasses `Circle` and `Rectangle`:
   - `Circle` has a field `radius` and implements `calculateArea()` to compute the area of a circle.
   - `Rectangle` has fields `length` and `width` and implements `calculateArea()` to compute the area of a rectangle.

3. In the `main()` method:
   - Create objects of `Circle` and `Rectangle` and call `calculateArea()` on each.

---

### **Exercise 3: Abstract Class with Constructor**
1. Create an abstract class `Vehicle` with:
   - A protected field `String brand`.
   - A constructor to initialize `brand`.
   - An abstract method `drive()`.

2. Create a subclass `Car` that extends `Vehicle`:
   - Implement `drive()` to print: `"Car is driving."`

3. In the `main()` method:
   - Create a `Car` object and call `drive()`. Also, print the `brand`.

---

### **Exercise 4: Interface Basics**
1. Create an interface `Flyable` with:
   - A method `fly()`.

2. Create classes `Bird` and `Plane` that implement `Flyable`:
   - Implement `fly()` in both classes to print: `"Bird is flying."` and `"Plane is flying."`

3. In the `main()` method:
   - Create objects of `Bird` and `Plane` and call `fly()` on each.

---

### **Exercise 5: Interface with Multiple Implementations**
1. Create an interface `Playable` with:
   - A method `play()`.

2. Create classes `Guitar` and `Piano` that implement `Playable`:
   - Implement `play()` in both classes to print messages like: `"Playing the guitar."` and `"Playing the piano."`

3. In the `main()` method:
   - Create an array of `Playable` type and store objects of `Guitar` and `Piano`.
   - Use a loop to call `play()` on each object.

---

### **Exercise 6: Interface with Default Method**
1. Create an interface `Printer` with:
   - A default method `printTestPage()` that prints: `"Printing a test page."`
   - An abstract method `printDocument(String doc)`.

2. Create a class `InkjetPrinter` that implements `Printer`:
   - Override `printDocument(String doc)` to print: `"Printing document: [doc]"`.

3. In the `main()` method:
   - Create an `InkjetPrinter` object and call both `printTestPage()` and `printDocument()`.

---

### **Exercise 7: Abstract Class vs. Interface**
1. Create an abstract class `Appliance` with:
   - A concrete method `powerOn()` that prints: `"Appliance is powered on."`
   - An abstract method `operate()`.

2. Create an interface `SmartDevice` with:
   - A method `connectToWiFi()`.

3. Create a class `SmartWashingMachine` that:
   - Extends `Appliance`.
   - Implements `SmartDevice`.
   - Overrides `operate()` to print: `"Washing clothes."`
   - Implements `connectToWiFi()` to print: `"Connected to WiFi."`

4. In the `main()` method:
   - Create a `SmartWashingMachine` object and call all its methods.

---

### **Exercise 8: Multiple Inheritance Using Interfaces**
1. Create two interfaces `Walkable` and `Runnable` with:
   - A method `walk()` in `Walkable`.
   - A method `run()` in `Runnable`.

2. Create a class `Athlete` that implements both interfaces:
   - Implement both `walk()` and `run()` to print respective messages.

3. In the `main()` method:
   - Create an `Athlete` object and call both `walk()` and `run()`.

---

### **Exercise 9: Interface Inheritance**
1. Create an interface `Readable` with:
   - A method `read()`.

2. Create another interface `Writable` that extends `Readable`:
   - Add a method `write()`.

3. Create a class `File` that implements `Writable`:
   - Implement both `read()` and `write()` to print respective messages.

4. In the `main()` method:
   - Create a `File` object and call both `read()` and `write()`.

---

### **Exercise 10: Abstract Class with Template Method**
1. Create an abstract class `Game` with:
   - A concrete method `start()` that prints: `"Game started."`
   - A concrete method `end()` that prints: `"Game ended."`
   - An abstract method `play()`.

2. Create subclasses `Cricket` and `Football`:
   - Override `play()` in `Cricket` to print: `"Playing Cricket."`
   - Override `play()` in `Football` to print: `"Playing Football."`

3. In the `main()` method:
   - Create objects of `Cricket` and `Football`.
   - Call `start()`, `play()`, and `end()` for each game.

---

These exercises provide comprehensive coverage of **Abstraction** using abstract classes and interfaces. Let me know if you’d like solutions for these!

## Here are the solutions to the exercises on **Abstraction (abstract classes and interfaces)** in Java:

---

### **Solution 1: Abstract Class Basics**
```java
abstract class Animal {
    abstract void makeSound();

    void eat() {
        System.out.println("Animal is eating.");
    }
}

class Dog extends Animal {
    @Override
    void makeSound() {
        System.out.println("Dog barks.");
    }
}

public class Main {
    public static void main(String[] args) {
        Dog dog = new Dog();
        dog.makeSound();
        dog.eat();
    }
}
```

---

### **Solution 2: Abstract Class with Multiple Subclasses**
```java
abstract class Shape {
    abstract void calculateArea();
}

class Circle extends Shape {
    double radius;

    Circle(double radius) {
        this.radius = radius;
    }

    @Override
    void calculateArea() {
        System.out.println("Area of Circle: " + (Math.PI * radius * radius));
    }
}

class Rectangle extends Shape {
    double length, width;

    Rectangle(double length, double width) {
        this.length = length;
        this.width = width;
    }

    @Override
    void calculateArea() {
        System.out.println("Area of Rectangle: " + (length * width));
    }
}

public class Main {
    public static void main(String[] args) {
        Shape circle = new Circle(5);
        Shape rectangle = new Rectangle(4, 6);

        circle.calculateArea();
        rectangle.calculateArea();
    }
}
```

---

### **Solution 3: Abstract Class with Constructor**
```java
abstract class Vehicle {
    protected String brand;

    Vehicle(String brand) {
        this.brand = brand;
    }

    abstract void drive();
}

class Car extends Vehicle {
    Car(String brand) {
        super(brand);
    }

    @Override
    void drive() {
        System.out.println(brand + " is driving.");
    }
}

public class Main {
    public static void main(String[] args) {
        Car car = new Car("Toyota");
        car.drive();
    }
}
```

---

### **Solution 4: Interface Basics**
```java
interface Flyable {
    void fly();
}

class Bird implements Flyable {
    @Override
    public void fly() {
        System.out.println("Bird is flying.");
    }
}

class Plane implements Flyable {
    @Override
    public void fly() {
        System.out.println("Plane is flying.");
    }
}

public class Main {
    public static void main(String[] args) {
        Flyable bird = new Bird();
        Flyable plane = new Plane();

        bird.fly();
        plane.fly();
    }
}
```

---

### **Solution 5: Interface with Multiple Implementations**
```java
interface Playable {
    void play();
}

class Guitar implements Playable {
    @Override
    public void play() {
        System.out.println("Playing the guitar.");
    }
}

class Piano implements Playable {
    @Override
    public void play() {
        System.out.println("Playing the piano.");
    }
}

public class Main {
    public static void main(String[] args) {
        Playable[] instruments = { new Guitar(), new Piano() };

        for (Playable instrument : instruments) {
            instrument.play();
        }
    }
}
```

---

### **Solution 6: Interface with Default Method**
```java
interface Printer {
    default void printTestPage() {
        System.out.println("Printing a test page.");
    }

    void printDocument(String doc);
}

class InkjetPrinter implements Printer {
    @Override
    public void printDocument(String doc) {
        System.out.println("Printing document: " + doc);
    }
}

public class Main {
    public static void main(String[] args) {
        InkjetPrinter printer = new InkjetPrinter();
        printer.printTestPage();
        printer.printDocument("Report.pdf");
    }
}
```

---

### **Solution 7: Abstract Class vs. Interface**
```java
abstract class Appliance {
    void powerOn() {
        System.out.println("Appliance is powered on.");
    }

    abstract void operate();
}

interface SmartDevice {
    void connectToWiFi();
}

class SmartWashingMachine extends Appliance implements SmartDevice {
    @Override
    void operate() {
        System.out.println("Washing clothes.");
    }

    @Override
    public void connectToWiFi() {
        System.out.println("Connected to WiFi.");
    }
}

public class Main {
    public static void main(String[] args) {
        SmartWashingMachine machine = new SmartWashingMachine();
        machine.powerOn();
        machine.operate();
        machine.connectToWiFi();
    }
}
```

---

### **Solution 8: Multiple Inheritance Using Interfaces**
```java
interface Walkable {
    void walk();
}

interface Runnable {
    void run();
}

class Athlete implements Walkable, Runnable {
    @Override
    public void walk() {
        System.out.println("Athlete is walking.");
    }

    @Override
    public void run() {
        System.out.println("Athlete is running.");
    }
}

public class Main {
    public static void main(String[] args) {
        Athlete athlete = new Athlete();
        athlete.walk();
        athlete.run();
    }
}
```

---

### **Solution 9: Interface Inheritance**
```java
interface Readable {
    void read();
}

interface Writable extends Readable {
    void write();
}

class File implements Writable {
    @Override
    public void read() {
        System.out.println("Reading the file.");
    }

    @Override
    public void write() {
        System.out.println("Writing to the file.");
    }
}

public class Main {
    public static void main(String[] args) {
        File file = new File();
        file.read();
        file.write();
    }
}
```

---

### **Solution 10: Abstract Class with Template Method**
```java
abstract class Game {
    void start() {
        System.out.println("Game started.");
    }

    void end() {
        System.out.println("Game ended.");
    }

    abstract void play();
}

class Cricket extends Game {
    @Override
    void play() {
        System.out.println("Playing Cricket.");
    }
}

class Football extends Game {
    @Override
    void play() {
        System.out.println("Playing Football.");
    }
}

public class Main {
    public static void main(String[] args) {
        Game cricket = new Cricket();
        cricket.start();
        cricket.play();
        cricket.end();

        Game football = new Football();
        football.start();
        football.play();
        football.end();
    }
}
```

---

These solutions demonstrate how to use **abstract classes** and **interfaces** effectively to implement abstraction in Java. Let me know if you need further explanations or variations!
