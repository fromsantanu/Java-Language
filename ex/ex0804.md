## Here are exercises focusing on the **finally block**, **throw and throws keywords**, and **custom exceptions** in Java:

---

### **Exercises: Finally Block**

#### **Exercise 1: Division with Finally**
1. Write a program to divide two numbers.
2. Use a `try-catch` block to handle `ArithmeticException`.
3. Use a `finally` block to print `"Execution completed."`.

---

#### **Exercise 2: File Handling with Finally**
1. Write a program to read a file named `example.txt`.
2. Use a `try-catch` block to handle `FileNotFoundException`.
3. Use a `finally` block to print `"File handling process finished."`.

---

#### **Exercise 3: Resource Cleanup**
1. Write a program to perform a database connection simulation.
2. Use a `try-catch` block to handle a simulated exception.
3. Use a `finally` block to simulate closing the database connection.

---

### **Exercises: Throw and Throws Keywords**

#### **Exercise 4: Throw Exception**
1. Write a program with a method `checkNumber(int num)` that:
   - Throws an `IllegalArgumentException` if the number is negative.
2. Call the method from `main()` and handle the exception.

---

#### **Exercise 5: Throws with Checked Exception**
1. Write a program with a method `readFile(String fileName)` that:
   - Uses `throws` to declare `IOException`.
   - Attempts to read a file and throws an exception if the file does not exist.
2. Handle the exception in the `main()` method.

---

#### **Exercise 6: Rethrow Exception**
1. Write a program where a method `processData()`:
   - Catches an exception and rethrows it using `throw`.
2. Handle the rethrown exception in the caller method.

---

#### **Exercise 7: Throws Multiple Exceptions**
1. Write a program with a method `performOperations(int a, int b)` that:
   - Declares `throws ArithmeticException, NullPointerException`.
   - Divides `a` by `b` and calls a method on a null object to simulate exceptions.
2. Handle both exceptions in the `main()` method.

---

### **Exercises: Custom Exceptions**

#### **Exercise 8: Custom Checked Exception**
1. Create a custom checked exception `InvalidAgeException`.
2. Write a program with a method `validateAge(int age)` that:
   - Throws `InvalidAgeException` if the age is less than 18.
3. Call the method from `main()` and handle the exception.

---

#### **Exercise 9: Custom Unchecked Exception**
1. Create a custom unchecked exception `NegativeValueException`.
2. Write a program with a method `checkValue(int value)` that:
   - Throws `NegativeValueException` if the value is less than 0.
3. Handle the exception in `main()`.

---

#### **Exercise 10: Custom Exception with Multiple Constructors**
1. Create a custom exception `InsufficientFundsException` with:
   - A default constructor.
   - A parameterized constructor that takes a custom message.
2. Write a program to simulate a bank withdrawal process:
   - If the withdrawal amount exceeds the balance, throw `InsufficientFundsException`.
   - Handle the exception in `main()`.

---

These exercises cover a wide range of concepts related to the **finally block**, **throw/throws keywords**, and **custom exceptions**. Let me know if you’d like solutions for these!

## Here are the solutions to the exercises on the **finally block**, **throw and throws keywords**, and **custom exceptions** in Java:

---

### **Solutions: Finally Block**

#### **Solution 1: Division with Finally**
```java
public class Main {
    public static void main(String[] args) {
        try {
            int a = 10, b = 0;
            int result = a / b;
        } catch (ArithmeticException e) {
            System.out.println("Error: Division by zero.");
        } finally {
            System.out.println("Execution completed.");
        }
    }
}
```

---

#### **Solution 2: File Handling with Finally**
```java
import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        try {
            File file = new File("example.txt");
            Scanner scanner = new Scanner(file);
            while (scanner.hasNextLine()) {
                System.out.println(scanner.nextLine());
            }
        } catch (FileNotFoundException e) {
            System.out.println("Error: File not found.");
        } finally {
            System.out.println("File handling process finished.");
        }
    }
}
```

---

#### **Solution 3: Resource Cleanup**
```java
public class Main {
    public static void main(String[] args) {
        try {
            System.out.println("Connecting to the database...");
            throw new RuntimeException("Database error occurred.");
        } catch (RuntimeException e) {
            System.out.println("Error: " + e.getMessage());
        } finally {
            System.out.println("Closing the database connection.");
        }
    }
}
```

---

### **Solutions: Throw and Throws Keywords**

#### **Solution 4: Throw Exception**
```java
public class Main {
    public static void checkNumber(int num) {
        if (num < 0) {
            throw new IllegalArgumentException("Number cannot be negative.");
        }
    }

    public static void main(String[] args) {
        try {
            checkNumber(-5);
        } catch (IllegalArgumentException e) {
            System.out.println("Caught Exception: " + e.getMessage());
        }
    }
}
```

---

#### **Solution 5: Throws with Checked Exception**
```java
import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class Main {
    public static void readFile(String fileName) throws FileNotFoundException {
        File file = new File(fileName);
        Scanner scanner = new Scanner(file);
        while (scanner.hasNextLine()) {
            System.out.println(scanner.nextLine());
        }
    }

    public static void main(String[] args) {
        try {
            readFile("example.txt");
        } catch (FileNotFoundException e) {
            System.out.println("Error: " + e.getMessage());
        }
    }
}
```

---

#### **Solution 6: Rethrow Exception**
```java
public class Main {
    public static void processData() throws Exception {
        try {
            throw new Exception("Simulated Exception.");
        } catch (Exception e) {
            System.out.println("Caught Exception in processData.");
            throw e; // Rethrowing the exception
        }
    }

    public static void main(String[] args) {
        try {
            processData();
        } catch (Exception e) {
            System.out.println("Rethrown Exception caught: " + e.getMessage());
        }
    }
}
```

---

#### **Solution 7: Throws Multiple Exceptions**
```java
public class Main {
    public static void performOperations(int a, int b) throws ArithmeticException, NullPointerException {
        int result = a / b; // May cause ArithmeticException
        String str = null;
        System.out.println(str.toUpperCase()); // May cause NullPointerException
    }

    public static void main(String[] args) {
        try {
            performOperations(10, 0);
        } catch (ArithmeticException e) {
            System.out.println("Caught ArithmeticException: " + e.getMessage());
        } catch (NullPointerException e) {
            System.out.println("Caught NullPointerException: " + e.getMessage());
        }
    }
}
```

---

### **Solutions: Custom Exceptions**

#### **Solution 8: Custom Checked Exception**
```java
class InvalidAgeException extends Exception {
    public InvalidAgeException(String message) {
        super(message);
    }
}

public class Main {
    public static void validateAge(int age) throws InvalidAgeException {
        if (age < 18) {
            throw new InvalidAgeException("Age must be 18 or older.");
        }
    }

    public static void main(String[] args) {
        try {
            validateAge(16);
        } catch (InvalidAgeException e) {
            System.out.println("Caught Exception: " + e.getMessage());
        }
    }
}
```

---

#### **Solution 9: Custom Unchecked Exception**
```java
class NegativeValueException extends RuntimeException {
    public NegativeValueException(String message) {
        super(message);
    }
}

public class Main {
    public static void checkValue(int value) {
        if (value < 0) {
            throw new NegativeValueException("Value cannot be negative.");
        }
    }

    public static void main(String[] args) {
        try {
            checkValue(-10);
        } catch (NegativeValueException e) {
            System.out.println("Caught Exception: " + e.getMessage());
        }
    }
}
```

---

#### **Solution 10: Custom Exception with Multiple Constructors**
```java
class InsufficientFundsException extends Exception {
    public InsufficientFundsException() {
        super("Insufficient funds.");
    }

    public InsufficientFundsException(String message) {
        super(message);
    }
}

public class Main {
    public static void withdraw(double balance, double amount) throws InsufficientFundsException {
        if (amount > balance) {
            throw new InsufficientFundsException("Attempted to withdraw " + amount + " but balance is " + balance);
        }
        System.out.println("Withdrawal successful: " + amount);
    }

    public static void main(String[] args) {
        try {
            withdraw(500.0, 600.0);
        } catch (InsufficientFundsException e) {
            System.out.println("Caught Exception: " + e.getMessage());
        }
    }
}
```

---

These solutions cover the **finally block**, **throw and throws keywords**, and **custom exceptions** comprehensively. Let me know if you need further clarifications or additional examples!
