# Exercises 
Here are a variety of exercises to help you practice **Recursion** in Java. The exercises range from basic to advanced to give you a comprehensive understanding of recursion.

---

### **1. Basic Recursion**

1. **Factorial Calculation**  
   Write a recursive function `factorial` that calculates the factorial of a given number.  
   - Example Input: `factorial(5)`  
   - Example Output: `120`

2. **Sum of Natural Numbers**  
   Write a recursive function `sumUpto` that calculates the sum of all natural numbers up to `n`.  
   - Example Input: `sumUpto(5)`  
   - Example Output: `15` (1 + 2 + 3 + 4 + 5)

3. **Power Calculation**  
   Write a recursive function `power` that calculates `base` raised to the power `exponent`.  
   - Example Input: `power(2, 3)`  
   - Example Output: `8`

---

### **2. Intermediate Recursion**

4. **Fibonacci Sequence**  
   Write a recursive function `fibonacci` to return the `n`th Fibonacci number.  
   - Example Input: `fibonacci(6)`  
   - Example Output: `8` (0, 1, 1, 2, 3, 5, 8...)

5. **Reverse a Number**  
   Write a recursive function `reverseNumber` to reverse the digits of a given number.  
   - Example Input: `reverseNumber(1234)`  
   - Example Output: `4321`

6. **Greatest Common Divisor (GCD)**  
   Write a recursive function `gcd` that finds the GCD of two numbers.  
   - Example Input: `gcd(48, 18)`  
   - Example Output: `6`

---

### **3. Recursive Patterns**

7. **Print Numbers in Increasing and Decreasing Order**  
   Write a recursive function `printOrder` that prints numbers from `n` down to `1` and then back up to `n`.  
   - Example Input: `printOrder(3)`  
   - Example Output: `3 2 1 2 3`

8. **Triangle Pattern**  
   Write a recursive function `printStars` that prints a triangle pattern of stars (`*`).  
   - Example Input: `printStars(3)`  
   - Example Output:  
     ```
     *
     **
     ***
     ```

---

### **4. Recursion with Strings**

9. **String Reversal**  
   Write a recursive function `reverseString` to reverse a given string.  
   - Example Input: `reverseString("hello")`  
   - Example Output: `"olleh"`

10. **Check Palindrome**  
    Write a recursive function `isPalindrome` to check if a given string is a palindrome.  
    - Example Input: `isPalindrome("radar")`  
    - Example Output: `true`

---

### **5. Recursive Numerical Problems**

11. **Count Digits**  
    Write a recursive function `countDigits` to count the number of digits in a given number.  
    - Example Input: `countDigits(12345)`  
    - Example Output: `5`

12. **Sum of Digits**  
    Write a recursive function `sumDigits` to calculate the sum of the digits of a given number.  
    - Example Input: `sumDigits(1234)`  
    - Example Output: `10`

13. **Find Minimum Digit in a Number**  
    Write a recursive function `findMinDigit` to find the smallest digit in a given number.  
    - Example Input: `findMinDigit(5724)`  
    - Example Output: `2`

---

### **6. Advanced Recursion**

14. **Tower of Hanoi**  
    Write a recursive function `towerOfHanoi` to solve the Tower of Hanoi problem for `n` disks. Print the steps to move the disks from source to destination using an auxiliary peg.  
    - Example Input: `towerOfHanoi(3, 'A', 'C', 'B')`  
    - Example Output:  
      ```
      Move disk 1 from A to C
      Move disk 2 from A to B
      Move disk 1 from C to B
      Move disk 3 from A to C
      Move disk 1 from B to A
      Move disk 2 from B to C
      Move disk 1 from A to C
      ```

15. **Generate Subsets**  
    Write a recursive function `generateSubsets` to generate all subsets of a given string.  
    - Example Input: `generateSubsets("abc")`  
    - Example Output: `["", "a", "b", "c", "ab", "ac", "bc", "abc"]`

16. **Print Permutations**  
    Write a recursive function `printPermutations` to generate all permutations of a given string.  
    - Example Input: `printPermutations("abc")`  
    - Example Output: `["abc", "acb", "bac", "bca", "cab", "cba"]`

---

### **7. Recursion with Decision Problems**

17. **N-Queens Problem**  
    Write a recursive function `solveNQueens` to find all solutions to the N-Queens problem for an `n × n` chessboard.

18. **Sudoku Solver**  
    Write a recursive function `solveSudoku` to solve a Sudoku puzzle by filling the grid with valid numbers.

---

### **8. Miscellaneous Recursion**

19. **Exponentiation by Squaring**  
    Write a recursive function `fastPower` to calculate the power of a number using exponentiation by squaring.  
    - Example Input: `fastPower(2, 10)`  
    - Example Output: `1024`

20. **Count Ways to Reach the Top of Stairs**  
    Write a recursive function `countWays` to calculate the number of ways to climb a staircase with `n` steps if you can take 1 or 2 steps at a time.  
    - Example Input: `countWays(4)`  
    - Example Output: `5`

---

These exercises cover a wide range of recursion concepts. Let me know if you’d like solutions to any of these!

# Exercises 

Here are the solutions to the **Programs with Recursion** exercises in Java:

---

### **1. Basic Recursion**

1. **Factorial Calculation**
   ```java
   public class Factorial {
       public int factorial(int n) {
           if (n == 0) return 1;
           return n * factorial(n - 1);
       }

       public static void main(String[] args) {
           Factorial f = new Factorial();
           System.out.println(f.factorial(5)); // Output: 120
       }
   }
   ```

2. **Sum of Natural Numbers**
   ```java
   public class SumOfNaturalNumbers {
       public int sumUpto(int n) {
           if (n == 0) return 0;
           return n + sumUpto(n - 1);
       }

       public static void main(String[] args) {
           SumOfNaturalNumbers s = new SumOfNaturalNumbers();
           System.out.println(s.sumUpto(5)); // Output: 15
       }
   }
   ```

3. **Power Calculation**
   ```java
   public class PowerCalculation {
       public int power(int base, int exponent) {
           if (exponent == 0) return 1;
           return base * power(base, exponent - 1);
       }

       public static void main(String[] args) {
           PowerCalculation p = new PowerCalculation();
           System.out.println(p.power(2, 3)); // Output: 8
       }
   }
   ```

---

### **2. Intermediate Recursion**

4. **Fibonacci Sequence**
   ```java
   public class Fibonacci {
       public int fibonacci(int n) {
           if (n <= 1) return n;
           return fibonacci(n - 1) + fibonacci(n - 2);
       }

       public static void main(String[] args) {
           Fibonacci f = new Fibonacci();
           System.out.println(f.fibonacci(6)); // Output: 8
       }
   }
   ```

5. **Reverse a Number**
   ```java
   public class ReverseNumber {
       public int reverseNumber(int n, int rev) {
           if (n == 0) return rev;
           return reverseNumber(n / 10, rev * 10 + n % 10);
       }

       public static void main(String[] args) {
           ReverseNumber r = new ReverseNumber();
           System.out.println(r.reverseNumber(1234, 0)); // Output: 4321
       }
   }
   ```

6. **Greatest Common Divisor (GCD)**
   ```java
   public class GCD {
       public int gcd(int a, int b) {
           if (b == 0) return a;
           return gcd(b, a % b);
       }

       public static void main(String[] args) {
           GCD g = new GCD();
           System.out.println(g.gcd(48, 18)); // Output: 6
       }
   }
   ```

---

### **3. Recursive Patterns**

7. **Print Numbers in Increasing and Decreasing Order**
   ```java
   public class PrintOrder {
       public void printOrder(int n) {
           if (n == 0) return;
           System.out.print(n + " ");
           printOrder(n - 1);
           System.out.print(n + " ");
       }

       public static void main(String[] args) {
           PrintOrder p = new PrintOrder();
           p.printOrder(3); // Output: 3 2 1 2 3
       }
   }
   ```

8. **Triangle Pattern**
   ```java
   public class TrianglePattern {
       public void printStars(int n) {
           if (n == 0) return;
           printStars(n - 1);
           for (int i = 0; i < n; i++) {
               System.out.print("*");
           }
           System.out.println();
       }

       public static void main(String[] args) {
           TrianglePattern t = new TrianglePattern();
           t.printStars(3);
           // Output:
           // *
           // **
           // ***
       }
   }
   ```

---

### **4. Recursion with Strings**

9. **String Reversal**
   ```java
   public class ReverseString {
       public String reverseString(String str) {
           if (str.isEmpty()) return "";
           return reverseString(str.substring(1)) + str.charAt(0);
       }

       public static void main(String[] args) {
           ReverseString r = new ReverseString();
           System.out.println(r.reverseString("hello")); // Output: olleh
       }
   }
   ```

10. **Check Palindrome**
    ```java
    public class PalindromeCheck {
        public boolean isPalindrome(String str) {
            if (str.length() <= 1) return true;
            if (str.charAt(0) != str.charAt(str.length() - 1)) return false;
            return isPalindrome(str.substring(1, str.length() - 1));
        }

        public static void main(String[] args) {
            PalindromeCheck p = new PalindromeCheck();
            System.out.println(p.isPalindrome("radar")); // Output: true
        }
    }
    ```

---

### **5. Recursive Numerical Problems**

11. **Count Digits**
    ```java
    public class CountDigits {
        public int countDigits(int n) {
            if (n == 0) return 0;
            return 1 + countDigits(n / 10);
        }

        public static void main(String[] args) {
            CountDigits c = new CountDigits();
            System.out.println(c.countDigits(12345)); // Output: 5
        }
    }
    ```

12. **Sum of Digits**
    ```java
    public class SumOfDigits {
        public int sumDigits(int n) {
            if (n == 0) return 0;
            return n % 10 + sumDigits(n / 10);
        }

        public static void main(String[] args) {
            SumOfDigits s = new SumOfDigits();
            System.out.println(s.sumDigits(1234)); // Output: 10
        }
    }
    ```

13. **Find Minimum Digit in a Number**
    ```java
    public class FindMinDigit {
        public int findMinDigit(int n, int min) {
            if (n == 0) return min;
            return findMinDigit(n / 10, Math.min(min, n % 10));
        }

        public static void main(String[] args) {
            FindMinDigit f = new FindMinDigit();
            System.out.println(f.findMinDigit(5724, Integer.MAX_VALUE)); // Output: 2
        }
    }
    ```

---

### **6. Advanced Recursion**

14. **Tower of Hanoi**
    ```java
    public class TowerOfHanoi {
        public void towerOfHanoi(int n, char source, char target, char auxiliary) {
            if (n == 0) return;
            towerOfHanoi(n - 1, source, auxiliary, target);
            System.out.println("Move disk " + n + " from " + source + " to " + target);
            towerOfHanoi(n - 1, auxiliary, target, source);
        }

        public static void main(String[] args) {
            TowerOfHanoi t = new TowerOfHanoi();
            t.towerOfHanoi(3, 'A', 'C', 'B');
        }
    }
    ```

15. **Generate Subsets**
    ```java
    public class GenerateSubsets {
        public void generateSubsets(String str, String current, int index) {
            if (index == str.length()) {
                System.out.println(current);
                return;
            }
            generateSubsets(str, current, index + 1);
            generateSubsets(str, current + str.charAt(index), index + 1);
        }

        public static void main(String[] args) {
            GenerateSubsets g = new GenerateSubsets();
            g.generateSubsets("abc", "", 0);
        }
    }
    ```

16. **Print Permutations**
    ```java
    public class PrintPermutations {
        public void printPermutations(String str, String result) {
            if (str.isEmpty()) {
                System.out.println(result);
                return;
            }
            for (int i = 0; i < str.length(); i++) {
                char ch = str.charAt(i);
                String rest = str.substring(0, i) + str.substring(i + 1);
                printPermutations(rest, result + ch);
            }
        }

        public static void main(String[] args) {
            PrintPermutations p = new PrintPermutations();
            p.printPermutations("abc", "");
        }
    }
    ```

---

These solutions demonstrate a wide range of recursion concepts. 
