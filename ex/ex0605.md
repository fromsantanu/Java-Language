# Here are exercises on the topic **"Arrays as Method Parameters"** =

---

### **Basic Exercises**

#### **Exercise 1: Sum of Array Elements**
1. Write a method `calculateSum(int[] arr)` that:
   - Accepts an array as a parameter.
   - Returns the sum of all elements in the array.
2. In the `main()` method:
   - Call the method with an example array and print the result.

---

#### **Exercise 2: Find Maximum Element**
1. Write a method `findMax(int[] arr)` that:
   - Accepts an array as a parameter.
   - Returns the maximum element in the array.
2. In the `main()` method:
   - Call the method with an example array and print the maximum value.

---

#### **Exercise 3: Reverse Array**
1. Write a method `reverseArray(int[] arr)` that:
   - Accepts an array as a parameter.
   - Reverses the array elements in place.
2. In the `main()` method:
   - Call the method with an example array and print the reversed array.

---

#### **Exercise 4: Print Array**
1. Write a method `printArray(String[] arr)` that:
   - Accepts a string array as a parameter.
   - Prints each element of the array on a new line.
2. In the `main()` method:
   - Call the method with an example string array.

---

### **Intermediate Exercises**

#### **Exercise 5: Element Search**
1. Write a method `searchElement(int[] arr, int key)` that:
   - Accepts an integer array and a key as parameters.
   - Returns the index of the key if it exists in the array, or `-1` if it doesn't.
2. In the `main()` method:
   - Call the method with an example array and key, and print the result.

---

#### **Exercise 6: Even and Odd Count**
1. Write a method `countEvenOdd(int[] arr)` that:
   - Accepts an array as a parameter.
   - Returns an array of size 2 where:
     - The first element is the count of even numbers.
     - The second element is the count of odd numbers.
2. In the `main()` method:
   - Call the method with an example array and print the result.

---

#### **Exercise 7: Sort Array**
1. Write a method `sortArray(int[] arr)` that:
   - Accepts an array as a parameter.
   - Sorts the array in ascending order.
2. In the `main()` method:
   - Call the method with an example array and print the sorted array.

---

#### **Exercise 8: Merge Two Arrays**
1. Write a method `mergeArrays(int[] arr1, int[] arr2)` that:
   - Accepts two arrays as parameters.
   - Returns a new array containing all elements from both arrays.
2. In the `main()` method:
   - Call the method with two example arrays and print the merged array.

---

### **Advanced Exercises**

#### **Exercise 9: Find Duplicates**
1. Write a method `findDuplicates(int[] arr)` that:
   - Accepts an array as a parameter.
   - Returns an array of duplicate elements in the input array.
2. In the `main()` method:
   - Call the method with an example array and print the duplicates.

---

#### **Exercise 10: Array to String**
1. Write a method `convertArrayToString(String[] arr)` that:
   - Accepts a string array as a parameter.
   - Joins all elements of the array into a single string separated by commas.
   - Returns the resulting string.
2. In the `main()` method:
   - Call the method with an example string array and print the resulting string.

---

#### **Exercise 11: Transpose Matrix**
1. Write a method `transposeMatrix(int[][] matrix)` that:
   - Accepts a 2D array (matrix) as a parameter.
   - Returns the transpose of the matrix.
2. In the `main()` method:
   - Call the method with an example matrix and print the transposed matrix.

---

#### **Exercise 12: Pass by Reference**
1. Write a method `incrementElements(int[] arr)` that:
   - Accepts an array as a parameter.
   - Increments each element of the array by 1.
2. In the `main()` method:
   - Call the method with an example array.
   - Print the array after calling the method to demonstrate pass-by-reference behavior.

---

#### **Exercise 13: Remove Element**
1. Write a method `removeElement(int[] arr, int key)` that:
   - Accepts an array and a key as parameters.
   - Returns a new array with all occurrences of the key removed.
2. In the `main()` method:
   - Call the method with an example array and key, and print the resulting array.

---

#### **Exercise 14: Split Array**
1. Write a method `splitArray(int[] arr, int index)` that:
   - Accepts an array and an index as parameters.
   - Splits the array into two arrays at the specified index and returns both arrays as a 2D array.
2. In the `main()` method:
   - Call the method with an example array and index, and print the two resulting arrays.

---

#### **Exercise 15: Array Rotation**
1. Write a method `rotateArray(int[] arr, int rotations)` that:
   - Accepts an array and a number of rotations as parameters.
   - Rotates the array to the right by the specified number of rotations.
2. In the `main()` method:
   - Call the method with an example array and rotations, and print the rotated array.

---

#### **Exercise 16: Find Pair with Sum**
1. Write a method `findPairWithSum(int[] arr, int target)` that:
   - Accepts an array and a target sum as parameters.
   - Returns an array of two numbers that add up to the target sum, or an empty array if no such pair exists.
2. In the `main()` method:
   - Call the method with an example array and target sum, and print the resulting pair.

---

#### **Exercise 17: Matrix Row Sum**
1. Write a method `rowSum(int[][] matrix)` that:
   - Accepts a 2D array (matrix) as a parameter.
   - Returns an array where each element represents the sum of the corresponding row.
2. In the `main()` method:
   - Call the method with an example matrix and print the row sums.

---

#### **Exercise 18: Array Element Frequency**
1. Write a method `elementFrequency(int[] arr)` that:
   - Accepts an array as a parameter.
   - Returns an array of unique elements and their respective frequencies in the format `{element1, freq1, element2, freq2, ...}`.
2. In the `main()` method:
   - Call the method with an example array and print the result.

---

These exercises cover a wide range of topics related to **arrays as method parameters** in Java, from basic array operations to advanced functionality. Let me know if you need solutions for these!

## Here are the solutions for the exercises on **Arrays as Method Parameters** in Java:

---

### **Basic Solutions**

#### **Solution 1: Sum of Array Elements**
```java
public class Main {
    public static int calculateSum(int[] arr) {
        int sum = 0;
        for (int value : arr) {
            sum += value;
        }
        return sum;
    }

    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5};
        System.out.println("Sum: " + calculateSum(arr));
    }
}
```

---

#### **Solution 2: Find Maximum Element**
```java
public class Main {
    public static int findMax(int[] arr) {
        int max = arr[0];
        for (int value : arr) {
            if (value > max) {
                max = value;
            }
        }
        return max;
    }

    public static void main(String[] args) {
        int[] arr = {10, 20, 30, 5, 15};
        System.out.println("Maximum: " + findMax(arr));
    }
}
```

---

#### **Solution 3: Reverse Array**
```java
import java.util.Arrays;

public class Main {
    public static void reverseArray(int[] arr) {
        int start = 0, end = arr.length - 1;
        while (start < end) {
            int temp = arr[start];
            arr[start] = arr[end];
            arr[end] = temp;
            start++;
            end--;
        }
    }

    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5};
        reverseArray(arr);
        System.out.println("Reversed Array: " + Arrays.toString(arr));
    }
}
```

---

#### **Solution 4: Print Array**
```java
public class Main {
    public static void printArray(String[] arr) {
        for (String element : arr) {
            System.out.println(element);
        }
    }

    public static void main(String[] args) {
        String[] arr = {"apple", "banana", "cherry"};
        printArray(arr);
    }
}
```

---

### **Intermediate Solutions**

#### **Solution 5: Element Search**
```java
public class Main {
    public static int searchElement(int[] arr, int key) {
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] == key) {
                return i;
            }
        }
        return -1;
    }

    public static void main(String[] args) {
        int[] arr = {10, 20, 30, 40, 50};
        System.out.println("Index of 30: " + searchElement(arr, 30));
    }
}
```

---

#### **Solution 6: Even and Odd Count**
```java
import java.util.Arrays;

public class Main {
    public static int[] countEvenOdd(int[] arr) {
        int even = 0, odd = 0;
        for (int value : arr) {
            if (value % 2 == 0) {
                even++;
            } else {
                odd++;
            }
        }
        return new int[]{even, odd};
    }

    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5};
        System.out.println("Even and Odd Counts: " + Arrays.toString(countEvenOdd(arr)));
    }
}
```

---

#### **Solution 7: Sort Array**
```java
import java.util.Arrays;

public class Main {
    public static void sortArray(int[] arr) {
        Arrays.sort(arr);
    }

    public static void main(String[] args) {
        int[] arr = {5, 3, 8, 1, 2};
        sortArray(arr);
        System.out.println("Sorted Array: " + Arrays.toString(arr));
    }
}
```

---

#### **Solution 8: Merge Two Arrays**
```java
import java.util.Arrays;

public class Main {
    public static int[] mergeArrays(int[] arr1, int[] arr2) {
        int[] merged = new int[arr1.length + arr2.length];
        System.arraycopy(arr1, 0, merged, 0, arr1.length);
        System.arraycopy(arr2, 0, merged, arr1.length, arr2.length);
        return merged;
    }

    public static void main(String[] args) {
        int[] arr1 = {1, 2, 3};
        int[] arr2 = {4, 5, 6};
        System.out.println("Merged Array: " + Arrays.toString(mergeArrays(arr1, arr2)));
    }
}
```

---

### **Advanced Solutions**

#### **Solution 9: Find Duplicates**
```java
import java.util.HashSet;

public class Main {
    public static int[] findDuplicates(int[] arr) {
        HashSet<Integer> seen = new HashSet<>();
        HashSet<Integer> duplicates = new HashSet<>();
        for (int value : arr) {
            if (!seen.add(value)) {
                duplicates.add(value);
            }
        }
        return duplicates.stream().mapToInt(Integer::intValue).toArray();
    }

    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 2, 4, 5, 1};
        int[] duplicates = findDuplicates(arr);
        System.out.println("Duplicates: " + Arrays.toString(duplicates));
    }
}
```

---

#### **Solution 10: Array to String**
```java
public class Main {
    public static String convertArrayToString(String[] arr) {
        return String.join(", ", arr);
    }

    public static void main(String[] args) {
        String[] arr = {"apple", "banana", "cherry"};
        System.out.println("Array as String: " + convertArrayToString(arr));
    }
}
```

---

#### **Solution 11: Transpose Matrix**
```java
import java.util.Arrays;

public class Main {
    public static int[][] transposeMatrix(int[][] matrix) {
        int rows = matrix.length;
        int cols = matrix[0].length;
        int[][] transposed = new int[cols][rows];

        for (int i = 0; i < rows; i++) {
            for (int j = 0; j < cols; j++) {
                transposed[j][i] = matrix[i][j];
            }
        }
        return transposed;
    }

    public static void main(String[] args) {
        int[][] matrix = {
            {1, 2, 3},
            {4, 5, 6},
            {7, 8, 9}
        };
        int[][] result = transposeMatrix(matrix);
        for (int[] row : result) {
            System.out.println(Arrays.toString(row));
        }
    }
}
```

---

#### **Solution 12: Pass by Reference**
```java
import java.util.Arrays;

public class Main {
    public static void incrementElements(int[] arr) {
        for (int i = 0; i < arr.length; i++) {
            arr[i]++;
        }
    }

    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5};
        incrementElements(arr);
        System.out.println("Modified Array: " + Arrays.toString(arr));
    }
}
```

---

#### **Solution 13: Remove Element**
```java
import java.util.Arrays;

public class Main {
    public static int[] removeElement(int[] arr, int key) {
        return Arrays.stream(arr).filter(value -> value != key).toArray();
    }

    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 2, 4, 5};
        int[] result = removeElement(arr, 2);
        System.out.println("Array after removal: " + Arrays.toString(result));
    }
}
```

---

#### **Solution 14: Split Array**
```java
import java.util.Arrays;

public class Main {
    public static int[][] splitArray(int[] arr, int index) {
        int[] firstPart = Arrays.copyOfRange(arr, 0, index);
        int[] secondPart = Arrays.copyOfRange(arr, index, arr.length);
        return new int[][]{firstPart, secondPart};
    }

    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5};
        int[][] result = splitArray(arr, 3);
        System.out.println("First Part: " + Arrays.toString(result[0]));
        System.out.println("Second Part: " + Arrays.toString(result[1]));
    }
}
```

---

#### **Solution 15: Array Rotation**
```java
import java.util.Arrays;

public class Main {
    public static void rotateArray(int[] arr, int rotations) {
        int n = arr.length;
        rotations %= n;
        int[] result = new int[n];
        for (int i = 0; i < n; i++) {
            result[(i + rotations) % n] = arr[i];
        }
        System.arraycopy(result, 0, arr, 0, n);
    }

    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5};
        rotateArray(arr, 2);
        System.out.println("Rotated Array: " + Arrays.toString(arr));
    }
}
```

---

These solutions cover all the exercises provided earlier, demonstrating how to use arrays as method parameters in Java. Let me know if you need further assistance!
