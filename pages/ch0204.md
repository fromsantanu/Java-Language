# **Operators in Java**

Operators in Java are special symbols or keywords used to perform operations on variables and values. Java supports a wide range of operators to handle arithmetic, logical, relational, and other tasks. This chapter provides an in-depth look at the various types of operators available in Java, along with their syntax, use cases, and examples.

---

## **1. Categories of Operators**

Java operators are broadly categorized as follows:

1. **Arithmetic Operators**
2. **Relational (Comparison) Operators**
3. **Logical Operators**
4. **Bitwise Operators**
5. **Assignment Operators**
6. **Unary Operators**
7. **Ternary (Conditional) Operator**
8. **Shift Operators**

---

## **2. Arithmetic Operators**

Arithmetic operators are used to perform basic mathematical operations like addition, subtraction, multiplication, and division.

| **Operator** | **Description**     | **Example**       | **Result**      |
|--------------|---------------------|-------------------|-----------------|
| `+`          | Addition            | `a + b`           | Sum of `a` and `b` |
| `-`          | Subtraction         | `a - b`           | Difference of `a` and `b` |
| `*`          | Multiplication      | `a * b`           | Product of `a` and `b` |
| `/`          | Division            | `a / b`           | Quotient of `a` and `b` |
| `%`          | Modulus (Remainder) | `a % b`           | Remainder when `a` is divided by `b` |

**Example:**
```java
int a = 10, b = 3;
System.out.println("Addition: " + (a + b));   // 13
System.out.println("Subtraction: " + (a - b)); // 7
System.out.println("Multiplication: " + (a * b)); // 30
System.out.println("Division: " + (a / b));   // 3
System.out.println("Modulus: " + (a % b));    // 1
```

---

## **3. Relational (Comparison) Operators**

Relational operators are used to compare two values and return a boolean (`true` or `false`).

| **Operator** | **Description**        | **Example** | **Result** |
|--------------|------------------------|-------------|------------|
| `==`         | Equal to               | `a == b`    | `true` if `a` equals `b` |
| `!=`         | Not equal to           | `a != b`    | `true` if `a` is not equal to `b` |
| `>`          | Greater than           | `a > b`     | `true` if `a` is greater than `b` |
| `<`          | Less than              | `a < b`     | `true` if `a` is less than `b` |
| `>=`         | Greater than or equal to| `a >= b`    | `true` if `a` is greater than or equal to `b` |
| `<=`         | Less than or equal to  | `a <= b`    | `true` if `a` is less than or equal to `b` |

**Example:**
```java
int a = 10, b = 20;
System.out.println(a == b); // false
System.out.println(a != b); // true
System.out.println(a > b);  // false
System.out.println(a < b);  // true
System.out.println(a >= b); // false
System.out.println(a <= b); // true
```

---

## **4. Logical Operators**

Logical operators are used to combine multiple boolean expressions.

| **Operator** | **Description**      | **Example**          | **Result**          |
|--------------|----------------------|----------------------|---------------------|
| `&&`         | Logical AND          | `a > 5 && b < 10`    | `true` if both conditions are true |
| `||`         | Logical OR           | `a > 5 || b < 10`    | `true` if at least one condition is true |
| `!`          | Logical NOT          | `!(a > 5)`           | Negates the condition |

**Example:**
```java
int a = 10, b = 5;
System.out.println(a > 5 && b < 10); // true
System.out.println(a > 5 || b > 10); // true
System.out.println(!(a > 5));        // false
```

---

## **5. Bitwise Operators**

Bitwise operators operate on bits and perform operations bit by bit.

| **Operator** | **Description** | **Example**  | **Result**      |
|--------------|-----------------|--------------|-----------------|
| `&`          | Bitwise AND     | `a & b`      | `1` if both bits are `1` |
| `|`          | Bitwise OR      | `a | b`      | `1` if at least one bit is `1` |
| `^`          | Bitwise XOR     | `a ^ b`      | `1` if bits are different |
| `~`          | Bitwise Complement | `~a`       | Inverts all bits |

**Example:**
```java
int a = 5;  // Binary: 0101
int b = 3;  // Binary: 0011
System.out.println(a & b); // 1 (Binary: 0001)
System.out.println(a | b); // 7 (Binary: 0111)
System.out.println(a ^ b); // 6 (Binary: 0110)
System.out.println(~a);    // -6 (Binary: 1010 in two's complement)
```

---

## **6. Assignment Operators**

Assignment operators are used to assign values to variables.

| **Operator** | **Description**  | **Example**  | **Result**        |
|--------------|------------------|--------------|-------------------|
| `=`          | Assign           | `a = b`      | Assigns `b` to `a`|
| `+=`         | Add and assign   | `a += b`     | `a = a + b`       |
| `-=`         | Subtract and assign | `a -= b`  | `a = a - b`       |
| `*=`         | Multiply and assign | `a *= b`  | `a = a * b`       |
| `/=`         | Divide and assign | `a /= b`    | `a = a / b`       |
| `%=`         | Modulus and assign | `a %= b`   | `a = a % b`       |

**Example:**
```java
int a = 10;
a += 5;  // a = 15
a *= 2;  // a = 30
System.out.println(a);
```

---

## **7. Unary Operators**

Unary operators operate on a single operand.

| **Operator** | **Description**         | **Example**  | **Result** |
|--------------|-------------------------|--------------|------------|
| `+`          | Positive                | `+a`         | Positive value of `a` |
| `-`          | Negative                | `-a`         | Negative value of `a` |
| `++`         | Increment (pre/post)    | `++a` or `a++` | Increases `a` by 1 |
| `--`         | Decrement (pre/post)    | `--a` or `a--` | Decreases `a` by 1 |
| `!`          | Logical NOT             | `!a`         | Negates a boolean value |

**Example:**
```java
int a = 10;
System.out.println(++a); // 11 (Pre-increment)
System.out.println(a--); // 11 (Post-decrement)
System.out.println(a);   // 10
```

---

## **8. Ternary (Conditional) Operator**

The ternary operator is a shorthand for `if-else` conditions.

**Syntax:**
```java
condition ? value_if_true : value_if_false
```

**Example:**
```java
int a = 10, b = 20;
int max = (a > b) ? a : b;
System.out.println("Maximum: " + max); // 20
```

---

## **9. Shift Operators**

Shift operators manipulate the bits of a number.

| **Operator** | **Description**           | **Example**  | **Result**            |
|--------------|---------------------------|--------------|-----------------------|
| `<<`         | Left shift                | `a << 2`     | Shift bits left by 2  |
| `>>`         | Right shift               | `a >> 2`     | Shift bits right by 2 |
| `>>>`        | Unsigned right shift      | `a >>> 2`    | Fill with zeros on the left |

**Example:**
```java
int a = 8; // Binary: 1000
System.out.println(a << 2); // 32 (Binary: 100000)
System.out.println(a >> 2); // 2 (Binary: 0010)
```

---

## **10. Example Program Using Multiple Operators**

```java
public class OperatorExample {
    public static void main(String[]

 args) {
        int a = 10, b = 5, c = 20;

        // Arithmetic
        System.out.println("Addition: " + (a + b));

        // Relational
        System.out.println("Is a greater than b? " + (a > b));

        // Logical
        System.out.println("Logical AND: " + (a > b && c > a));

        // Bitwise
        System.out.println("Bitwise AND: " + (a & b));

        // Ternary
        int max = (a > b) ? a : b;
        System.out.println("Maximum: " + max);
    }
}
```

---

## **11. Conclusion**

Operators are fundamental to Java programming, enabling various computations, comparisons, and logic handling. Mastery of arithmetic, relational, logical, bitwise, and other operators is essential for writing effective and optimized Java programs. By combining different operators, you can solve complex problems and enhance your application's functionality.
